
# End-to-End Insurance Risk Analytics

This repository contains the data engineering pipeline, reproducible environment setup, and initial exploratory analyses developed for an auto-insurance portfolio. The project leverages 18 months of historical claims data to evaluate data quality, isolate risk indicators, and establish a version-controlled workflow.

---

## Objectives

The primary analytical and operational goals for this phase of the project are:

* **Establish a Reproducible Development Workflow:** Set up a clean repo structure, an automated CI/CD linting pipeline, and isolated local version controls for all data states.
* **Formulate a Baseline Risk Profile:** Ingest historical policy, vehicle, and client information to understand overall portfolio profitability via core insurance performance metrics.
* **Assess and Remediate Data Quality:** Profile structural missingness, apply logical cleanups to implicit data types, and prepare stable data features for downstream mathematical modeling.

---

## Directory Structure

```text
insurance-risk-analytics/
├── .github/
│   └── workflows/
│       └── unittests.yml       
├── data/                      
├── notebooks/
│   └── 01_insurance_eda.ipynb  
├── src/
│   ├── __init__.py       
│   └── insurance_eda.py        
├── .dvc/                       
├── .gitignore                                 
├── requirements.txt
├── reports/                                
├── tests/          
└── README.md

```

---

## Implementation Details

### Task 1: Environment Setup & Exploratory Data Analysis (EDA)

The modular analysis class (`src/insurance_eda.py`) handles statistical profiling and data hygiene tasks sequentially:

* **Descriptive Statistics & Baseline Profiling:** Extracts numerical summaries (`.describe().T`) and evaluates structural column formatting.
* **Data Quality & Schema Adjustment:** Screens for structural missingness (flagging parameters with over 50% blank fields). It typecasts dates (`TransactionMonth`, `VehicleIntroDate`) and normalizes messy string representations of currency objects into structured floats.
* **Domain-Specific Insurance Cleanup:** Maps implicit binary flags (`WrittenOff`, `Rebuilt`, `Converted`) to strict integer indicators and addresses demographic gaps via mode imputation. It eliminates rows missing critical registration keys (`make`, `Model`, `mmcode`) and optimizes categorical columns into standard memory-efficient category types.
* **Foundational Financial Evaluation:** Computes core insurance portfolio indicators across geographical distributions:

$$\text{Loss Ratio} = \frac{\text{TotalClaims}}{\text{TotalPremium}}$$

$$\text{Margin} = \text{TotalPremium} - \text{TotalClaims}$$

* **Statistical Visual Profiling:** Generates univariate feature distributions, constructs global correlation matrices, aggregates premium mapping variations across postal zones, and applies log-scaled box plots to evaluate risk-skewing outliers.

### Task 2: Data Version Control (DVC)

To ensure strict analytical reproducibility and maintain audit trails without bloating the Git history, the data directory is managed via DVC:

* **Isolated Pipeline Management:** The data directory contains a target `.gitignore` automatically generated by DVC, preventing large text inputs from being pushed to GitHub.
* **State Versioning:** Captures data pipeline progression by decoupling and versioning distinct tracking states (e.g., raw ingestion versus fully resolved and cast data outputs).
* **Local Storage Integration:** Links project revisions directly to independent target directory pointer remotes (`dvc remote add -d localstorage "C:\Users\Hello\Documents\Insurance_dvc"`), tracking data updates locally via secure pointer files (`.dvc`).

---

## Data Pipeline Ingestion & Production Setup

To recreate the exact data environment locally:

### 1. Synchronize Project Files & Dependencies

```bash
git clone https://github.com/soliana-k/insurance-risk-analytics
cd insurance-risk-analytics
pip install -r requirements.txt

```

### 2. Configure Local Storage Storage & Pull Data

Ensure your local system path maps to the dedicated external storage folder, then fetch the active dataset binaries:

```bash
dvc remote add -d localstorage "C:\Users\Hello\Documents\Insurance_dvc"
dvc pull

```

*DVC matches the specific cryptographic pointer values stored within `data/MachineLearningRating_v3.txt.dvc` and pulls the corresponding data binary straight out of your local tracking vault.*


I use DVC to track different stages of the data for reproducibility.

**Tracked Versions:**
- `data/raw/MachineLearningRating_v3.txt` → Original raw data
- `data/processed/cleaned_insurance_data.csv` → Cleaned and preprocessed version

**To reproduce:**
```bash
dvc pull

```
### Task 3: A/B Hypothesis Testing

To validate risk drivers, we conducted statistical tests on four key null hypotheses ($H_0$). We segmented the portfolio into baseline and test groups to determine if observed differences were statistically significant.

#### Statistical Test Results

| Hypothesis ($H_0$) | KPI | Test Used | $p$-value | Decision |
| --- | --- | --- | --- | --- |
| No risk difference by Province | Freq | Chi-Squared | $1.38 \times 10^{-15}$ | **REJECT $H_0$** |
| No risk difference by Zip Code | Sev | $t$-test | $0.700$ | **FAIL TO REJECT** |
| No margin diff by Zip Code | Margin | $t$-test | $0.246$ | **FAIL TO REJECT** |
| No risk difference by Gender | Freq | Chi-Squared | $6.65 \times 10^{-5}$ | **REJECT $H_0$** |

#### Business Recommendations

* **Regional Pricing:** We found Gauteng has a significantly higher claim frequency (60% higher than Western Cape). **Recommendation:** Implement a regional risk-loading factor for premiums in high-density urban areas.
* **Demographic Segmentation:** Men exhibit a 43% higher claim frequency than women. **Recommendation:** Incorporate gender-based frequency risk factors into the dynamic pricing model to ensure premiums reflect actual portfolio experience.

---

### Task 4: Statistical Modeling & Risk-Based Pricing

We implemented a two-part pricing framework: a **Frequency Model** (to predict claim probability) and a **Severity Model** (to predict payout cost).

#### Model Comparison Table

| Model | Classification: F1-Score | Regression: RMSE | Regression: $R^2$ |
| --- | --- | --- | --- |
| **Linear Baseline** | 0.0198 | 35,802.77 | 0.17 |
| **Random Forest** | 0.0207 | 36,060.84 | 0.16 |
| **XGBoost Engine** | **0.0217** | 36,035.61 | 0.16 |

#### Pricing Framework

The final optimized premium is calculated as:


$$\text{Premium} = (P(\text{claim}) \times \text{Predicted Severity}) + \text{Expense Loading (20\%)} + \text{Profit Margin (10\%)}$$

#### Interpretability & Feature Drivers

Using **SHAP**, we identified the top drivers of risk. The most impactful feature, `SumInsured`, correlates directly with severity, providing quantitative evidence that higher-value assets require higher premium baselines to cover potential total-loss scenarios.

---

## Reproducibility

* **Modeling Code:** All model training, tuning, and evaluation scripts are located in `src/modeling.py`.
* **Hypothesis Testing:** Statistical validation scripts are found in `src/hypothesis_tests.py`.
* **Workflow:** Run `python notebooks/task_4_modeling.ipynb` to regenerate the pricing matrix and feature importance charts.

---

